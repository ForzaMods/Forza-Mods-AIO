<Window x:Class="WPF_Mockup.Tabs.AIO_Info.Overlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_Mockup.Tabs.AIO_Info"
        mc:Ignorable="d"
        Title="Overlay" Height="129" Width="200" Topmost="True" Background="{x:Null}"
        WindowStyle="None"
        AllowsTransparency="True" ShowInTaskbar="False" Loaded="Window_Loaded" Focusable="False" FontWeight="Bold">
    <Grid IsHitTestVisible="False">
        <Grid.RowDefinitions>
            <RowDefinition x:Name="TopSection" Height="15"/>
            <RowDefinition x:Name="MainSection"/>
            <RowDefinition x:Name="BottomSection" Height="15"/>
        </Grid.RowDefinitions>
        <Border x:Name="Border" Grid.RowSpan="3" Grid.Column="1" BorderBrush="Black" BorderThickness="5,5,5,5"/>
        <TextBlock x:Name="OptionsBlock"
                       Grid.Row="1"
                       TextWrapping="Wrap"
                       Text="Options" FontFamily="Manrope" Foreground="Black" FontSize="24"
                       HorizontalAlignment="Center" VerticalAlignment="Top"
                       TextAlignment="Center"/>
    </Grid>
</Window>
